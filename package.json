{
  "name": "sdb-portal",
  "private": true,
  "packageManager": "pnpm@9.15.9",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "format": "prettier --write .",
    "start": "node apps/api/dist/server.js",
    "build:api": "pnpm --filter @sdb/contracts build && pnpm --filter @sdb/api build"
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "supabase": "^2.0.0"
  }
}
